<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">


    <CheckBoxPreference
        android:key="example_checkbox"
        android:title="@string/pref_title_social_recommendations"
        android:summary="@string/pref_description_social_recommendations"
        android:defaultValue="true" />

    <PreferenceCategory
            android:title="@string/pref_title_location_settings"
            android:key="@string/pref_location_settings_key">
    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
            <EditTextPreference
                android:key="@string/pref_zip_code_entry_key"
                android:title="@string/pref_zip_code_entry_title"
                android:hint="@string/pref_enter_zip_code"
                android:defaultValue="02115"
                android:selectAllOnFocus="true"
                android:inputType="number"
                android:singleLine="true"
                android:maxLines="1"
                android:maxLength="5"/>
    </PreferenceCategory>

    <PreferenceCategory
            android:title="@string/pref_unit_title"
            android:key="@string/pref_unit_key">
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
        <ListPreference
            android:key="@string/pref_pick_unit_key"
            android:title="@string/pref_pick_unit_title"
            android:defaultValue="Fahrenheit"
            android:entries="@array/pref_temp_list_titles"
            android:entryValues="@array/pref_temp_list_values"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null" />
    </PreferenceCategory>

</PreferenceScreen>
